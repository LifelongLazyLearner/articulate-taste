# Taste profile opt-in

This project uses the `{{SKILL_NAME}}` profile.

Scope: everything in this repository.

Narrow it by replacing that line, for example "only `src/` and the public
docs", or "only prose, not code". The skill reads this file before it reads
the profile, and stays quiet about anything outside the scope named here.

Delete this file to opt the project back out.
